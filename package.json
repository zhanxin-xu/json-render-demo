{
  "name": "json-render-demo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "export:component-schemas": "node scripts/export-component-json-schemas.cjs",
    "setup:githooks": "git config core.hooksPath .githooks"
  }
}
