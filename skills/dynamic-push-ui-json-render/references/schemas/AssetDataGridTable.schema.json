{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "tableColumns": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "columnKey": {
            "type": "string",
            "description": "Unique key for the table column."
          },
          "columnTitle": {
            "type": "string",
            "description": "Header title for the table column."
          },
          "columnStyle": {
            "description": "Optional display style for values in this column: default = standard centered text, badge = rendered as a rounded status chip, accent = rendered with accent color and stronger emphasis.",
            "type": "string",
            "enum": [
              "default",
              "badge",
              "accent"
            ]
          }
        },
        "required": [
          "columnKey",
          "columnTitle"
        ],
        "additionalProperties": false,
        "description": "Definition of one data table column."
      },
      "description": "Column definitions for the data table."
    },
    "tableRows": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "rowKey": {
            "type": "string",
            "description": "Stable key for the row."
          },
          "rowName": {
            "type": "string",
            "description": "Display name shown in the first column."
          },
          "cells": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "columnKey": {
                  "type": "string",
                  "description": "Column key that this cell belongs to."
                },
                "cellValue": {
                  "type": "string",
                  "description": "Display value of the table cell."
                }
              },
              "required": [
                "columnKey",
                "cellValue"
              ],
              "additionalProperties": false,
              "description": "One cell value keyed by column."
            },
            "description": "Cell values mapped by column keys."
          }
        },
        "required": [
          "rowKey",
          "rowName",
          "cells"
        ],
        "additionalProperties": false,
        "description": "One row in the generic data grid table."
      },
      "description": "Row entries for the data table."
    },
    "firstColumnHeader": {
      "description": "Optional header for the first fixed column.",
      "type": "string"
    }
  },
  "required": [
    "tableColumns",
    "tableRows"
  ],
  "additionalProperties": false,
  "description": "Props for the reusable data grid table."
}
