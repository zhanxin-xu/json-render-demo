{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "actions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "negative",
              "positive"
            ],
            "description": "Button type: negative = white negative feedback button, positive = green positive action button."
          },
          "attributes": {
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {},
            "description": "Button attributes map without strict value typing."
          }
        },
        "required": [
          "type",
          "attributes"
        ],
        "additionalProperties": false,
        "description": "One action button configuration in the notification footer."
      },
      "description": "Ordered button actions rendered in the footer."
    }
  },
  "required": [
    "actions"
  ],
  "additionalProperties": false,
  "description": "Props for the notification button list component."
}
